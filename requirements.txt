# VOXAR Python Engine — Requirements (LOCKED for v2 build)
# Install with: pip install -r requirements.txt
# Target: Python 3.10, CUDA 12.1, RunPod A5000
#
# NOTE: TTS==0.22.0 is installed in Dockerfile with --no-deps
# because it hard-pins numpy==1.22.0 which librosa 0.10.x bans.
# This is a known Coqui bug never fixed before shutdown.

# numpy 1.22.4 — avoids librosa ban on 1.22.0/1.22.1/1.22.2
numpy==1.22.4

# Core AI
torch==2.2.2
torchaudio==2.2.2

# Audio processing
pydub>=0.25.1
noisereduce>=3.0.0
scipy==1.11.4
librosa==0.10.1
soundfile>=0.12.0
pyloudnorm>=0.1.1

# NLP — MUST stay below 4.40 (4.40+ removed BeamSearchScorer)
transformers==4.38.2
# OpenVoice installed manually:
# pip install git+https://github.com/myshell-ai/OpenVoice.git

# Speech-to-text
faster-whisper==1.0.3

# API server
fastapi>=0.109.0
uvicorn[standard]>=0.25.0
python-multipart>=0.0.6

# TTS transitive deps (needed because TTS installed with --no-deps)
cython>=0.29.30
scikit-learn>=1.3.0
numba>=0.57.0
inflect>=5.6.0
tqdm>=4.64.1
anyascii>=0.3.0
pyyaml>=6.0
fsspec>=2023.6.0
aiohttp>=3.8.1
packaging>=23.1
flask>=2.0.1
pysbd>=0.3.4
umap-learn>=0.5.1
pandas>=1.4,<2.0
matplotlib>=3.7.0
trainer>=0.0.32
coqpit>=0.0.16
jieba
pypinyin
hangul-romanize
gruut[de,es,fr]==2.2.3
jamo
nltk
g2pkk>=0.1.1
bangla
bnnumerizer
bnunicodenormalizer
einops>=0.6.0
encodec>=0.1.1
unidecode>=1.3.2
num2words
spacy[ja]>=3
