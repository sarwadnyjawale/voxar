# VOXAR Python Engine — Requirements
# Python 3.10 · CUDA 11.8 · PyTorch 2.1 (provided by base image)
#
# torch and torchaudio are NOT listed here — they ship inside the
# RunPod base image.  Re-installing them would overwrite the CUDA
# build with a CPU-only wheel and break GPU inference.

# ── Core AI ──
TTS==0.22.0
transformers>=4.36.0,<5
accelerate>=0.25.0

# ── Audio processing ──
numpy>=1.24.0,<2
scipy>=1.11.0,<1.14
pydub>=0.25.1
noisereduce>=3.0.0
librosa>=0.10.0,<0.11
soundfile>=0.12.0
pyloudnorm>=0.1.1
ffmpeg-python>=0.2.0

# ── Speech-to-text ──
faster-whisper>=1.0.0

# ── API server ──
fastapi>=0.109.0
uvicorn[standard]>=0.25.0
python-multipart>=0.0.6
