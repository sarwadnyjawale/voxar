# Python
__pycache__/
*.pyc
*.pyo
*.pyd
venv/

# Node / Frontend
node_modules/
.next/
out/
dist/
build/
coverage/

# Output / Generated
output/
logs/
voices/
# BUT track the catalog, embeddings, and scripts needed at runtime
!voices/voices_catalog.json
!voices/preview_scripts.json
!voices/embeddings/
!voices/embeddings/**

# Audio / Media (general — voice embeddings exempted above)
*.wav
*.mp3
# BUT keep voice embedding WAVs (small reference files, ~1MB each)
!voices/embeddings/*.wav

# Binaries
*.exe

# Environment Variables
.env
.env.*
backend/.env
backend/.env.*
frontend/.env

# Secrets / Ops
ops/api_keys.local.md
ops/secrets.local.md
ops/voxar_secrets_master.txt
ops/railway_env_template.txt

# Uploads
backend/uploads/

# IDE / Tool
.aider*
.claude/